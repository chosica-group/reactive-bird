// import { DICT_PATTERNS } from './validationDict';

// export const validateInput = (input: Record<string, unknown>) => {
//   const currentInput = DICT_PATTERNS[input.name];
//   const isValidInput: boolean = currentInput.regexp.test(input.value);
//   return isValidInput;
// };
